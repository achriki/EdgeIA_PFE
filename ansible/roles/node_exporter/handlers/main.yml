---
# roles/node_exporter/handlers/main.yml

- name: reload_systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart_node_exporter
  ansible.builtin.systemd:
    name: node_exporter
    state: restarted
