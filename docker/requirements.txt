# requirements.txt 
# Pin every version. Unpinned deps = non-reproducible builds.
# On arm/v7 some wheels don't exist on PyPI → pip builds from


# Core inference runtime
numpy==1.26.4  # arm/v7 wheels available for 1.24.x
opencv-python-headless==4.10.0.84  # headless = no GUI deps (saves ~30 MB)
PyYAML==6.0.1 # for loading dataset.yaml / configs

# TFLite runtime (replaces full TensorFlow — ~2 MB vs ~400 MB)
# The correct wheel is auto-selected by pip based on platform tag.
tflite-runtime==2.14.0

# Inference API
flask==3.0.3
gunicorn==22.0.0

# Metrics exposure for Prometheus (used by Grafana dashboard)
prometheus-client==0.20.0

# Pillow for image pre/post-processing
Pillow==10.3.0